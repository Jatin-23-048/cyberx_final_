name: digital_defence
description: "Digital Defence - Real-Time Threat Intelligence & User Safety"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management & Reactive
  provider: ^6.1.5
  
  # Local Storage (Web compatible)
  shared_preferences: ^2.3.2
  
  # UI & Design
  google_fonts: ^6.2.1
  curved_navigation_bar: ^1.0.3
  flutter_animate: ^4.5.0
  
  # Utilities
  intl: ^0.19.0
  uuid: ^4.0.0
  
  # Icons
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
